# Assignment 2 — Match Database

Database-style assignment for storing and querying match records (precursor to the more advanced Cricket Match Database in Assignment 3, which uses nested AVL trees and lives as its own standalone repo).

**Status:** Completed in an earlier working session — only the test file (`test_a2.py`) has been directly reviewed so far. Add the actual source file (`DSA_Assignment02_281131349_A.py`) here.
