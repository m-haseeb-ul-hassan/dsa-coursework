# BST + Library Catalogue Lab Set

An earlier iteration of the BST + Library Catalogue work (separate from Lab 10/11 above), consisting of `bst.py`, `bst_node.py`, `main.py`, and `library.py`. Two versions of the BST file exist across old uploads, likely from two different lab attempts/iterations.

**Status:** Completed in an earlier working session. Before adding to this repo, compare the two existing `bst.py` versions and keep only the correct/final one to avoid duplication with Lab 10/11.
