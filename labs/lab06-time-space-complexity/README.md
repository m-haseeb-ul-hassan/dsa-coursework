# Lab 6 — Time and Space Complexity Analysis

Paper-based analysis of 8 Python programs: finding the largest element, array rotation (reversal algorithm), factorial (recursive), binary search, Fibonacci (recursive, with recursion tree), recursive list sum, division-by-2 loop counting, and Tower of Hanoi. Each requires both time and space (recursion stack) complexity derivations.

**Status:** Theory/paper-based — no code deliverable.
