# Lab 8 — Queue Implementation

Covers a `Queue` class (enqueue, dequeue, front, is_empty, size) backed by a Python list, plus `QueueUsingStacks`, which simulates FIFO behavior using two LIFO stacks.

**Status:** In progress — starter file only had method stubs (`pass`/`TODO`). Being completed.
