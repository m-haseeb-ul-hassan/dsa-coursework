# Lab 13 — Heaps (Max-Heap)

A `MaxHeap` class backed by a Python list, split across `max_heap.py` (implementation) and `main.py` (testing), following the same modular pattern as the other labs.

## Methods implemented
`_heapify_up`, `_heapify_down`, `insert`, `extract_max`, `build_heap` (heapify an arbitrary unsorted list in place), plus `peek`, `size`, `display`.

**Status:** Completed in an earlier working session — add the finished `max_heap.py` and `main.py` here (verified against expected output for insert, extract_max, and build_heap test cases).
