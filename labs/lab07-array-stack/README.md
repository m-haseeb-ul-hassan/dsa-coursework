# Lab 7 — Arrays and Stack Implementation

Covers a custom `Array` class (insert, delete, search, update, display, size) backed by a Python list, a `Stack` class (push, pop, peek, is_empty, size), a `find_max` function, and reversing an array using stack operations.

**Status:** In progress — starter file only had method stubs (`pass`/`TODO`). Being completed.
