# Lab 11 — BST Deletion & Real-World Application

Extends Lab 10's BST with a `delete()` method covering all three deletion cases (leaf, one child, two children via in-order successor), then applies the completed BST to a real scenario: a `LibraryCatalogue` class (in `library.py`) ordered by ISBN, supporting add, remove, find, and sorted listing of books.

**Status:** In progress — starter files only had method stubs (`pass`). Being completed.
